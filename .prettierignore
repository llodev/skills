# dependencies
node_modules/

# secrets — never format env files (.env.example is fine to format)
.env
.env.*
!.env.example

# lockfile (prettier already skips by default; explicit for clarity)
pnpm-lock.yaml
