# @llodev/skills — Roadmap

Date: 2026-07-16. Baseline: `main` at core v1.14.0 / asana v1.9.0 / trello v1.8.0 / jira v1.2.0 / linear v1.1.0 / meta v3.1.0 (PRs #52–#60 merged). Forward-looking roadmap — single source of truth for **what's next**. Per-package `CHANGELOG.md` (auto-generated by Changesets) is the canonical history; this doc stays focused on the future.

Sections §1–§8 cover the `pm-tasks-*` family. The repo is now multi-family — new skill families beyond `pm-tasks-*` are tracked in **§9** (families: `django-*`, `ts-ddd-*`).

**Active P0 plan:** none hard-committed. Adapter expansion (jira S1, linear S2) and the cross-adapter **Lifecycle Fidelity** program are both complete — all 5 released adapters (asana, trello, jira, linear, plus core) now share create-time typed `dueDate` and start/close temporal handling. The next priority is stabilizing and dogfooding the 5 released adapters rather than starting a 6th; see §3.

> [!NOTE]
> When an item ships, **remove it** from this doc — don't strikethrough. Old context lives in `CHANGELOG.md` + git log.

---

## 1. Recently delivered

Last few releases + the program that just closed. Full history lives in each package's `CHANGELOG.md`.

**Lifecycle Fidelity (PRs #52–#60):** cross-adapter program bringing create-time typed `dueDate` and start/close temporal handling to parity across all 5 released adapters. Native no-overwrite close for asana (`completed_at`), jira (`resolutiondate`), and linear (`startedAt`/`completedAt`); Trello is the mirror image — overwrites `due` at close and preserves the plan in a description footer. Design spec: [`docs/superpowers/specs/2026-07-14-lifecycle-fidelity-design.md`](superpowers/specs/2026-07-14-lifecycle-fidelity-design.md).

| Release              | Theme                             | Outcome                                                                                                                                                                                                                  |
| -------------------- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| linear v1.1.0 (#60)  | Lifecycle Fidelity R4 (Linear)    | `@llodev/pm-tasks-linear@1.1.0` — create-time `dueDate` mapping + native `startedAt`/`completedAt` documented; closes the Lifecycle Fidelity program (core → Asana → Trello → Jira → Linear).                            |
| jira v1.2.0 (#58)    | Lifecycle Fidelity R4 (Jira)      | `@llodev/pm-tasks-jira@1.2.0` — create-time `duedate` mapping + native `resolutiondate` no-overwrite close documented.                                                                                                   |
| trello v1.8.0 (#56)  | Lifecycle Fidelity R3 (Trello)    | `@llodev/pm-tasks-trello@1.8.0` — create-time `due` mapping + overwrite-`due`/description-footer close (the non-native mirror of asana/jira/linear).                                                                     |
| asana v1.9.0 (#54)   | Lifecycle Fidelity R2 (Asana)     | `@llodev/pm-tasks-asana@1.9.0` — create-time `due_on` mapping + native `completed_at` no-overwrite close documented.                                                                                                     |
| core v1.14.0 (#52)   | Lifecycle Fidelity R1 (core)      | `@llodev/pm-tasks-core@1.14.0` — optional create-time fields (`labels`, `priority`, `estimate`, `dueDate`) added to `TaskCreateRequest`; `references/lifecycle-fidelity.md` documents the create→start→close principle.  |
| jira / linear v1.0.0 | First releases (S1 / S2 adapters) | `@llodev/pm-tasks-jira` and `@llodev/pm-tasks-linear` shipped as the first two new adapters post platform-hardening; jira bundled `task.parent.set`/`task.estimate.set`; linear later added `task.sprint.set` (v1.13.0). |

---

## 2. Open gaps (pick-from list)

### 2.1 Tests & quality

| #   | Gap                                                | Why                                              | Effort |
| --- | -------------------------------------------------- | ------------------------------------------------ | ------ |
| E5  | **Mutation testing** on `init-lib` + testkit fakes | Tests could weaken silently as the surface grows | M      |

### 2.3 Observability

| #   | Gap                                                                 | Why                           | Effort |
| --- | ------------------------------------------------------------------- | ----------------------------- | ------ |
| O2  | **Opt-in telemetry** (which verbs / which adapters / failure rates) | Data-driven roadmap decisions | M      |

### 2.4 Skill behavior

| #   | Gap                                                                                                                                                                                                                                                                                                 | Why                                                                                                                                                                                                                                                                                                                                                               | Effort |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------ |
| U1  | **Language-respect enforcement** — `.<tool>.json language` field must dictate agent-side narration (comments, autonomous-mode commit/PR copy, audit-log free-text). The **plan's language** still dictates task title/description content (separate concern). Currently drifts to English silently. | Observed across v1.6.0–v1.8.0 sessions: agent-side narration, commits, and PRs landed in English even though the workspace + plan were Portuguese. Mitigation: surface `language` to the agent on skill activation (banner in SKILL.md), add a probe in `pm-tasks-core-doctor` that warns when recent audit entries' free-text language doesn't match the config. | S      |

### 2.5 Blocked on upstream

| #   | Item                                                                                                                        | Blocker                                                                                                                                                                                                                                                                                                                            | Unblocks                                                                                                                                                |
| --- | --------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- |
| B1  | **Asana native `estimated_time_minutes`** support in `pm-tasks-asana` (mapped through the shipped `task.estimate.set` verb) | Claude Asana MCP doesn't expose the field natively yet — only `start_on` / `due_on` / `start_at` / `due_at` + `custom_fields`. A `custom_fields` workaround is **out of scope**: pm-tasks has no verb for arbitrary custom-field writes; the existing flow only fills fields declared in `.asana.json`. Ship native or not at all. | When the MCP exposes the field (or Anthropic publishes a typed schema update), bundle the mapping with `task.estimate.set` as a `pm-tasks-asana` minor. |

---

## 3. Next P0 — stabilize the 5 released adapters

Adapter expansion (jira S1, linear S2) and the **Lifecycle Fidelity** program are both delivered — all 5 released adapters (asana, trello, jira, linear + core) share create-time typed `dueDate` and start/close temporal handling, and the canonical verb set is complete at 10 (§5). There is no hard-committed 6th adapter or next version yet.

The honest next priority is **stabilizing and dogfooding the 5 released adapters** — real-world usage surfaces gaps faster than speculative feature work. Candidates while dogfooding runs: **U1** language-respect (§2.4, S) and **F13** batch card+checklist create (§6). The remaining scaffolds — `pm-tasks-{clickup,notion,monday,bitrix24,todoist}` plus the `github-projects` note (§4) — are a demand-driven pool: the next one to build is whichever gets pulled by actual need, not a pre-picked slot.

---

## 4. New adapters (demand-driven pool)

jira (S1) and linear (S2) shipped — no longer listed here. The rest is a demand-driven pool, not a committed sequence: pick whichever gets pulled by actual need next, per §3. Versions are unassigned until one is picked up.

| #       | Skill                                 | Market rationale                                                              | MCP status                            | Effort                                         |
| ------- | ------------------------------------- | ----------------------------------------------------------------------------- | ------------------------------------- | ---------------------------------------------- |
| S8      | **`pm-tasks-github-projects`**        | Native PM on GitHub; direct PR/issue link; pure dev audience                  | `github-mcp-server` covers it         | M                                              |
| S3      | **`pm-tasks-clickup`**                | Fastest-growing in SMB / agencies / hybrid teams                              | ClickUp MCP community maturing        | M (lists, custom statuses, rich custom fields) |
| S4      | **`pm-tasks-notion`**                 | Largest install base; databases as PM. Covers the "doesn't buy Jira" audience | Notion MCP official GA                | M (relational databases, dynamic properties)   |
| S5      | **`pm-tasks-monday`**                 | Strong in non-dev enterprise (ops, marketing, HR)                             | Monday MCP in development             | M                                              |
| S6      | **`pm-tasks-todoist`**                | Largest consumer base (~30M); freelancer / solo. Simple model                 | Todoist MCP community stable          | S                                              |
| S7      | **`pm-tasks-bitrix24`**               | LATAM / Eastern Europe / SMB-CRM niche                                        | No official MCP; well-documented REST | M                                              |
| S9-S11  | `pm-tasks-{height,basecamp,airtable}` | Niche / smaller TAM                                                           | mixed                                 | M each                                         |
| S12-S13 | `pm-tasks-{wrike,smartsheet}`         | Enterprise but low mindshare; overlaps with Monday                            | mixed                                 | M-L                                            |

---

## 5. New canonical verbs (cross-adapter features)

**The base+optional canonical verb set is complete at 10** (7 base + `task.parent.set`, `task.estimate.set`, `task.sprint.set` — all shipped, see `skills/pm-tasks-core/references/contract.md:72-94`). Remaining candidates below are additive Phase A verbs not yet bundled with a concrete adapter need.

Each verb is additive — Phase A of `contract-check` allows minor releases. Bundled with the adapter that first needs it.

| #   | Verb candidate                | Trigger adapter                   | Description                            |
| --- | ----------------------------- | --------------------------------- | -------------------------------------- |
| F5  | `task.time.log` (11th)        | Jira / Linear / ClickUp / Todoist | Native time-tracking                   |
| F6  | `task.blocks.add` (12th)      | Jira / Linear / Monday            | Dependency graph (blocks / blocked-by) |
| F10 | `task.wip-limit.check` (13th) | Trello / Jira / Linear            | WIP limits enforcement                 |

---

## 6. Library / SDK work

Features that live outside the adapter-by-adapter cadence.

| #   | Item                                                                                                 | Description                                                                                                        | Depends on           |
| --- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------ | -------------------- |
| F13 | **Batch card+checklists creation** (custom namespaced `<adapter>.task.batch-create-with-checklists`) | Wraps parallel POSTs / Trello `idChecklistSource` template; ~10× faster on large plans                             | —                    |
| P2  | **`@llodev/pm-tasks-cli`** standalone CLI                                                            | Consumes adapters via dynamic `import()` (the F14 headless `/adapter` subpath). Useful outside IDE / agent context | F14 (shipped v1.9.0) |
| P4  | **Plugin SDK** — export `contract.test.mjs` reusable suite as `npx pm-tasks-contract-tests`          | Raises ecosystem quality without forcing PRs into the monorepo                                                     | —                    |

---

## 7. Reverse-sync & analytics (hold)

Heavy items that only make sense once ≥4 mature adapters exist. Holding pen.

| #   | Item                                                  | Why hold                                                      | Effort |
| --- | ----------------------------------------------------- | ------------------------------------------------------------- | ------ |
| F1  | Reverse sync / read-back (bidirectional)              | Architectural change; spec must work across multiple adapters | L      |
| F4  | Cross-tool migration (`pm-tasks-migrate from=X to=Y`) | Maps via core vocabulary — needs ≥4 adapters worth of verbs   | L      |
| F8  | Card templates                                        | Generic feature; lands once adapters stabilize                | S      |
| F9  | Multi-board orchestration                             | Single plan distributed across multiple boards/projects       | M      |
| F11 | Standup notes verb (`task.standup.post`)              | 24h aggregated summary across adapters                        | M      |
| F12 | Velocity / burndown reporter                          | Read-only analytics over audit log + tool APIs                | M      |

---

## 8. Risks

| Risk                                                                           | Mitigation                                                                                                                                         |
| ------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| A tool's MCP goes down or changes contract                                     | E2E canary covers tarball install + smoke against real npm (canary publish, v1.10.0). Add a live-MCP smoke per adapter alongside each new adapter. |
| Tarball size explodes with N adapters × M locales                              | Tarball snapshot test (v1.2.1) catches drift; size-limit gate (shipped v1.7.0) makes the budget explicit                                           |
| Verb surface grows past `task.move` (7) → harder to audit autonomous allowlist | `crud-vocabulary.md` is canonical; each new verb goes through `contract-check` Phase A; allowlist in `.tool.json` stays explicit                   |
| Skill-judge baseline drifts as the rubric evolves                              | Rubric golden master + drift gate (shipped v1.7.0) snapshots rubric input next to score                                                            |

---

## 9. Beyond pm-tasks — new skill families

The repo is no longer single-family. `pm-tasks-*` stays the primary track; new families ship as new skills under the flat `skills/` directory (one directory per skill, the convention skill indexers expect), with family membership carried by the name prefix and each skill's `metadata.family` field rather than by directory nesting. Each skill versions independently (own `CHANGELOG.md`, own changeset, branch = version) and is registered in `pnpm-workspace.yaml` + `.claude-plugin/marketplace.json`. The `marketplace-parity` gate resolves each package dir from its `source.path`, so it's layout-agnostic.

### `django-*` — Django design knowledge skills

Pure knowledge skills (no MCP / config / init) that inject expert Django decisions — trade-offs, anti-patterns, decision trees — rather than basic ORM syntax. Each skill owns exactly one design concern so they compose without overlapping coverage.

| Skill                  | Concern                                                                                                                                                           | Status              |
| ---------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------- |
| `django-schema-design` | Database schema — PK strategy (incremental / UUIDv4 / UUIDv7 with insert-locality trade-offs), indexes (composite/covering/partial), constraints, safe migrations | ✅ shipped `v0.1.0` |
| `django-model-design`  | Model layer — relationships, managers/querysets, fat-vs-thin models, business-logic placement                                                                     | 🔜 planned          |

The `schema-design` / `model-design` split is deliberate: DB-level decisions vs. ORM/model-layer decisions, no dimension covered twice. Further Django-context skills are planned — names/scope to be locked with the maintainer before each lands (remove from this table once shipped, per the top-of-doc rule).

### `ts-ddd-*` — TypeScript DDD design knowledge skills

Project-agnostic Domain-Driven Design guides for TypeScript codebases — one skill per architectural layer (entity, value object, DTO, use case, repository, controller, domain service) plus a read-side CQRS companion. Pure knowledge skills (no MCP / config / init): they activate on the prompt and inject expert decisions (`Result`-based validation, `cloneWith` state transitions, enum-backed closed sets, Firestore/InMemory adapter pairs) rather than basic TypeScript syntax.

| Skill                     | Concern                                                                                              | Status              |
| ------------------------- | ---------------------------------------------------------------------------------------------------- | ------------------- |
| `ts-ddd-entity`           | Domain entities — `Entity` base, `create`/`tryCreate` with `Result.combine`, `cloneWith` transitions | ✅ shipped `v0.1.0` |
| `ts-ddd-value-object`     | Value objects — closed-set and composite VOs, `tryCreate`/`create`, normalization                    | ✅ shipped `v0.1.0` |
| `ts-ddd-dto`              | DTOs & contracts — Zod 4 schemas paired with `z.infer` types, enum-backed closed sets                | ✅ shipped `v0.1.0` |
| `ts-ddd-use-case`         | Application use cases — `UseCase<IN,OUT>`, repo-port orchestration, `Result.ok`/`fail`/`combine`     | ✅ shipped `v0.1.0` |
| `ts-ddd-repository`       | Repository ports + adapters — Firestore/InMemory pair, DI token, contract tests                      | ✅ shipped `v0.1.0` |
| `ts-ddd-controller`       | HTTP controllers — routes, guards, Zod validation, `Result`→HTTP mapping                             | ✅ shipped `v0.1.0` |
| `ts-ddd-domain-service`   | Domain services — stateless policies/calculators spanning entities, returning `Result`               | ✅ shipped `v0.1.0` |
| `ts-query-cqrs`           | Read-side CQRS — `*Query` ports, `find-*` read use cases, DTO projections, pagination/filters        | ✅ shipped `v0.1.0` |
| `@llodev/ts-ddd` _(meta)_ | Installs the whole `ts-ddd-*` family in one command                                                  | ✅ shipped `v0.1.0` |
