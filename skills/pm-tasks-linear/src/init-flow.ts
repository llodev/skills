/**
 * Linear init flow — Phase 3 placeholder.
 *
 * This module will be replaced in Phase 3 with a full interactive init
 * that enumerates your Linear workspace, team, states, members, labels,
 * and estimation settings, then writes a `.linear.json` config file.
 */

export async function runInit(_opts?: { outPath?: string }): Promise<void> {
  throw new Error("pm-tasks-linear init not yet implemented (Phase 3)");
}
